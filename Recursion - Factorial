#include<stdio.h>

    int digitofnumber(int num){
        if(num==0){
            return 0;
        }
    return 1 + digitofnumber(num/10);
    }

int main(){

    int num;

    printf("Enter number:");
    scanf("%d", &num);

    printf("%d", digitofnumber(num));

return 0;
}
