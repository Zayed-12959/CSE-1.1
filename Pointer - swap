#include <stdio.h>

void swap(int *a, int *b){
    int temp = *a;
    *a = *b;
    *b = temp;
return;
}

int main() {

    int a = 2;
    int b = 7;

    printf("Before swapping:\na=%d, b=%d\n", a, b);

    swap(&a,&b);

    printf("After swapping:\na=%d, b=%d", a, b);

return 0;
}

