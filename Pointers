#include<stdio.h>

int main(){

    int x = 5;
    int y = 10;

    int *p1;
    p1 = &x;
    int *p2 = &y;

    printf("Value of x : %d\n", x);
    printf("Address of x: %d\n", &x);
    printf("Address of p1: %d\n", p1);
    printf("Content of p1: %d\n", *p1);
    printf("Address of p: %d\n", &p1);

return 0;
}
