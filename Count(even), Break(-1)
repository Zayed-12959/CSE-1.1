Write a program that counts the number of even numbers entered by the user. the program should:
1)continuously accept integers as input
2)stop accepting input when the user inputs -1
3)output the count of even numbers entered

#include<stdio.h>

int main(){

    int n;
    int count = 0;

    printf("Enter numbers(enter-1tobreak)=");

    for( ; ; ){
        scanf("%d", &n);

        if(n==-1){
            break;
        }
        if(n%2==0){
            count++;
        }
    }
    printf("The count of even numbers entered: %d", count);

return 0;
}
