(1) The length and height of a rectangle and radius of a circle are input through the keyboard. Write a program to find the area & perimeter of the rectangle and the area & circumference of the circle.

#include<stdio.h>

int main(){

    int length, height, radius;

    printf("Enter length:");
    scanf("%d", &length);
    printf("Enter height:");
    scanf("%d", &height);
    printf("Enter radius:");
    scanf("%d", &radius);

    printf("Area of rectangle:%d\n", length*height);
    printf("Perimeter of rectangle:%d\n", 2*(length + height));
    printf("Area of circle:%.2f\n", 3.14*(float)radius*(float)radius);
    printf("Circumference of circle:%.2f", 2*3.14*(float)radius);

return 0;
}

(2) Rahim’s basic salary is input through the keyboard. His House rent allowance is 30% of basic salary and medical allowance is 5% of basic salary. He gets extra 1000 tk as technical allowance. Write a program to calculate his gross salary and print the result.

#include<stdio.h>

int main(){

    int basic,houserent,medical,technical;

    printf("Basic Salary:");
    scanf("%d", &basic);

    houserent= (float)basic*(30/100);
    medical= (float)basic*(5/100);
    technical= 1000;

    printf("Gross Salary:%d", basic+houserent+technical+medical);

return 0;
}

(3) The distance between Dhaka and Rajshahi (in km) is input through keyboard. Write a program to convert and print this distance in meters, feet, inches and centimeters.

#include<stdio.h>

int main(){

    int distance;
    float meter, feet, inches, centimeter;

    printf("Enter distance(km) between Dhaka and Rajshahi:");
    scanf("%d", &distance);

    meter = (float)distance*1000;
    feet = (float)distance*3280.84;
    inches = (float)distance*39370.1;
    centimeter = (float)distance*100000;

    printf("Meter:%.2f\nFeet:%.2f\nInches:%.2f\nCentimeters:%.2f", meter,feet,inches,centimeter);

return 0;
}

(4)  Temperature of a city in Fahrenheit degrees is input through the keyboard. Write a program to convert this temperature into centigrade degrees.

#include<stdio.h>

int main(){

    int F;
    float C;

    printf("Enter the temperature of a city in Fahrenheit:");
    scanf("%d", &F);

    C= (((float)F-32)/9)*5;

    printf("The temperature in Celcius scale is:%.2f", C);

return 0;
}

(5) Two numbers are input through the keyboard into two locations A and B. Write a program to interchange the contents of A and B.

#include<stdio.h>

int main(){

    int a,b,temp;

    printf("Enter 2 numbers:");
    scanf("%d%d", &a,&b);

    printf("Numbers before swapping:\na=%d\nb=%d\n", a, b);

    temp= a;
    a= b;
    b= temp;

    printf("Numbers after swapping:\na=%d\nb=%d", a, b);

return 0;
}

(6)  If marks obtained by a student in 5 different subjects are input from keyboard, find out the aggregate marks and percentage marks obtained by the student.

#include<stdio.h>

int main(){

    double mark[5];
    double total= 0.0;

    for(int i=0; i<5; i++){
        printf("Enter marks for subject%d:", i+1);
        scanf("%lf", &mark[i]);
        total = total + mark[i];
    }

    printf("Aggregate marks:%.2lf", total);
    printf("Percentage:%.2lf", (total/500)*100);

return 0;
}

(7)  If a 5 digit number is input through the keyboard, write a program to calculate and print the sum of its digits. [Hint: Use the modulus operator ‘%’

#include<stdio.h>

int main(){

    int number,digit;
    int sum = 0;

    printf("Enter a 5 digit number:");
    scanf("%d", &number);

    for( ; number!=0; number=number/10){
        digit = number%10;
        sum = sum + digit;
    }

    printf("The sum of the digits:%d", sum);

return 0;
}

(8) If a 5 digit number is input through the keyboard, write a program to reverse the number

#include<stdio.h>

int main(){

    int number,digit;
    int reverse = 0;

    printf("Enter a 5 digit number:");
    scanf("%d", &number);

    for( ; number!=0; number=number/10){
        digit = number%10;
        reverse = reverse*10+digit;
    }

    printf("The reversed number:%d", reverse);

return 0;
}

(9)  If a 4 digit number is input through the keyboard, write a program to obtain the sum of the first and last digit of this number.

#include <stdio.h>

int main() {
    int number, first, last;

    printf("Enter a 4-digit number: ");
    scanf("%d", &number);

    last = number % 10;

    first = number / 1000;

    printf("Sum of the first and last digit: %d\n", first + last);

    return 0;
}

(10) Determine the largest among three numbers.

#include<stdio.h>

int main(){

    int a,b,c;

    printf("Enter 3 numbers:");
    scanf("%d%d%d", &a,&b,&c);

    if(a>=b){
        if(a>=c){
            printf("%d is the largest", a);
        }
        else{
            printf("%d is the largest", c);
        }
    }
    else if(b>=c){
        printf("%d is the largest", b);
    }
    else{
        printf("%d is the largest", c);
    }

return 0;
}

(11)  Take a year as input and determine whether it is a leap year . [Hint: Check the divisibility by 4, 100 and 400

#include<stdio.h>

int main(){

    int year;

    printf("Enter a year:");
    scanf("%d", &year);

    if(year%400==0 || (year%4==0 && year%100!=0)){
        printf("%d is a leap year.\n", year);
    }
    else{
        printf("%d is not a leap year.");
    }

return 0;
}

(12)  If cost price and selling price of an item is input through the keyboard, write a program to determine whether the seller has made profit or incurred loss. Also determine how much profit he made or loss he incurred.

#include<stdio.h>

int main(){

    int cost, sell;

    printf("Enter Cost:");
    scanf("%d", &cost);

    printf("Enter Selling price:");
    scanf("%d", &sell);

    if(cost>sell){
        printf("Loss:%d", cost-sell);
    }
    else if(sell>cost){
        printf("Profit:%d", sell-cost);
    }
    else{
        printf("Np profit.");
    }

return 0;
}

(13)  Any integer is input through keyboard. Write a program to find out whether it is an odd number or even number .

#include<stdio.h>

int main(){

    int a;

    printf("Enter an integer:");
    scanf("%d", &a);

    if(a%2==0){
        printf("Even");
    }
    else if(a%2!=0){
        printf("Odd");
    }
    else{
        printf("Invalid");
    }

return 0;
}

(14) 
AUST grading policy is : 

(i) 80 % marks or above is A+ 
(ii) 75% to 79% marks is A 
(iii) 70% to 74% marks is A- 
(iv) 65% to 69% marks is B+ 
(v) 60% to 64% marks is B 
(vi) 55% to 59% marks is B- 
(vii) 50% to 54% marks is C+ 
(viii) 45% to 49% marks is C 
(ix) 40% to 44% marks is D 
(x) Below 40% is F 

Write a program which will take an input from user and calculate the grade of a student according to AUST grading policy based on that input

#include<stdio.h>

int main(){

    int marks;

    printf("Enter marks:");
    scanf("%d", &marks);

    if(marks<40){
        printf("Failed");
    }
    else if(marks>=40 && marks<=44){
        printf("D");
    }
    else if(marks>=45 && marks<=49){
        printf("C");
    }
    else if(marks>=50 && marks<=54){
        printf("C+");
    }
    else if(marks>=55 && marks<=59){
        printf("B-");
    }
    else if(marks>=60 && marks<=64){
        printf("B");
    }
    else if(marks>=65 && marks<=69){
        printf("B+");
    }
    else if(marks>=70 && marks<=74){
        printf("A-");
    }
    else if(marks>=75 && marks<=79){
        printf("A");
    }
    else if(marks>=80 && marks<=100){
        printf("A+");
    }

return 0;
}

(15)   

A certain grade of steel is graded according to the following conditions: 

(i) Hardness must be greater than 60
(ii) Carbon content must be less than 0.7 
(iii) Tensile strength must be greater than 5000 

The grades are as follows: 
Grade is 10 if all three conditions are met 
Grade is 9 if condition (i) and (ii) are met 
Grade is 8 if condition (ii) and (iii) are met 
Grade is 7 if condition (i) and (iii) are met 
Grade is 6 if only one condition is met 
Grade is 5 if none of the conditions are met 

Write a program which will require the user to give values of hardness, carbon content and tensile strength of the steel under consideration and output the grade of the steel.

#include<stdio.h>

int main(){

    int hardness,tensile;
    float carbon;

    printf("Enter Hardness:");
    scanf("%d", &hardness);
    printf("Enter carbon content:");
    scanf("%f", &carbon);
    printf("Enter tensile strength:");
    scanf("%d", &tensile);

    if(hardness>60 && carbon<0.7 && tensile>5000){
        printf("Grade 10");
    }
    else if(hardness>60 && carbon<0.7){
        printf("Grade 9");
    }
    else if(carbon<0.7 && tensile>5000){
        printf("Grade 8");
    }
    else if(hardness>60 && tensile>5000){
        printf("Grade 7");
    }
    else if(hardness>60 || carbon<0.7 || tensile>5000){
        printf("Grade 6");
    }
    else{
        printf("Grade 5");
    }

return 0;
}

#include<stdio.h>

long long power(int base, int exp){
    long long result = 1;

    for(int i=1; i<=exp; i++){
        result= result*base;
    }
    return result;
}

long long factorial(int num){
    long long fact=1;

    for(int i=1; i<=num; i++){
        fact = fact*i;
    }
    return fact;
}

(16)  x and n are input through keyboard. Write a program to compute x n , n! , n C r , n P r.

int main(){

    int x, n, r;
    int xn, nfact, nCr, nPr;

    printf("x:");
    scanf("%d", &x);
    printf("n:");
    scanf("%d", &n);
    printf("r:");
    scanf("%d", &r);

    xn= power(x,n);
    printf("Power x^n= %lld\n", xn);

    nfact= factorial(n);
    printf("Factorial n!= %lld\n", nfact);

    if(r<=n){
        nCr= factorial(n)/(factorial(r)*(factorial(n-r)));
    }
    else{
        nCr = 0;
    }
    printf("nCr = %lld\n", nCr );

    if(r<=n){
        nPr= factorial(n)/factorial(n-r);
    }
    else{
        nPr = 0;
    }
    printf("nPr = %lld\n", nPr);

return 0;
}

(17) Write a program to determine the GCD (greatest common divisor) and LCM (least common multiple) of 3 numbers.

#include<stdio.h>
int main(){

    int a,b,c,gcd,lcm;

    printf("Enter 3 numbers:");
    scanf("%d%d%d", &a,&b,&c);

    for(int i=1; i<=a && i<=b && i<=c; ++i){
        if(a%i==0 && b%i==0 && c%i==0){
            gcd=i;
        }
    }
    printf("GCD of %d %d and %d is %d\n", a,b,c,gcd);

    int max, m;
    m=a*b*c;
    if(a>=b && a>c){
        max =a;
    }
    else if(b>a && b>c){
        max = b;
    }
    else{
        max = c;
    }

    for(int i=max; i<=m; ++i){
        if(i%a==0 && i%b==0 && i%c==0){
            lcm = i;
        }
    }
    printf("LCM of %d %d and %d is %d", a,b,c,lcm);

    return 0;
}

(18)  Write a program to determine all prime numbers within the range [a …b] where a & b are input through keyboard.

#include <stdio.h>
#include <math.h>

int isPrime(int num){
    if(num <= 1)
        return 0;
    for(int i = 2; i <= sqrt(num); i++){
        if(num % i == 0)
        return 0;
    }
    return 1;
}

int main(){
    int a,b;
    printf("Enter the range [a, b]:\n");
    printf("a: ");
    scanf("%d", &a);
    printf("b: ");
    scanf("%d", &b);

    printf("Prime numbers in the range [%d, %d]:\n", a, b);
    for(int i = a; i <= b; i++){
        if(isPrime(i)){
            printf("%d ", i);
        }
    }
    printf("\n");

    return 0;
}

(19)   Construct the following table. Here n is input from the user. 
1 2 3 … n 
2 4 6 … 2n 
3 6 9 … 3n 
. . . … 
. . . . … 
. . . . … 
n 2n 3n … nn

#include<stdio.h>

int main(){

int n;

printf("Enter the value of n:");
scanf("%d", &n);

for(int i = 1; i<=n; i++){
    for(int j = 1; j<=n; j++){
        printf("%d\t", i*j);
    }
    printf("\n");
}

return 0;
}

(20)    Write a program to find out first n perfect number where n is the input from user.
        (A perfect number is a positive integer that is equal to the sum of its proper divisors (excluding itself).)

#include <stdio.h>

int main(){

    int i, j, end, sum;
    printf("Enter upper limit: ");
    scanf("%d", &end);

    for(i=1; i<=end; i++){

        sum = 0;
        for(j=1; j<i; j++){
            if(i % j == 0){
            sum += j;
            }
        }
        if(sum == i){
            printf("%d, ", i);
        }
    }
return 0;
}

(21)    Write a program to find first n Fibonacci number where n is the input from user.

#include<stdio.h>
int main(){

    int n, a=0, b=1,c;

    printf("Enter the number of terms:");
    scanf("%d", &n);

    for(int i=1; i<=n; i++){
        printf("%d ",a);
        c=a+b;
        a=b;
        b=c;
    }

return 0;
}

(22)
    *
   ***
  *****
 *******
*********

*********
 *******
  *****
   ***
    *

    *
   ***
  *****
 *******
*********
 *******
  *****
   ***
    *

#include<stdio.h>
int main(){

int n;
printf("Enter a number:");
scanf("%d", &n);

//EQUILATERAL
for(int i=1; i<=n; i++){
    for(int j=1; j<=(n-i); j++){
        printf(" ");
    }
    for(int k=1; k<=(i*2)-1; k++){
        printf("*");
    }
    printf("\n");
}

// REVERSE EQUILATERAL
for(int i=1; i<=n ; i++){ //when two triangles merged here int i = 2
    for(int j=1; j<=(i-1); j++){
        printf(" ");
    }
    for(int k=1; k<=(2*n)-(2*i-1);k++){
        printf("*");
    }
    printf("\n");
}

//DIAMOND SHAPE
for(int i=1;i<=n; i++){
    for(int j=1;j<=(n-i); j++){
        printf(" ");
    }
    for(int k=1;k<=(2*i)-1; k++){
        printf("*");
    }
    printf("\n");
}
for(int i=2; i<=n ; i++){
    for(int j=1; j<=(i-1); j++){
        printf(" ");
    }
    for(int k=1; k<=(2*n)-(2*i-1);k++){
        printf("*");
    }
    printf("\n");
}

//HOURGLASS
for(int i=1; i<=n ; i++){ //when two triangles merged here int i = 2
    for(int j=1; j<=(i-1); j++){
        printf(" ");
    }
    for(int k=1; k<=(2*n)-(2*i-1);k++){
        printf("*");
    }
    printf("\n");
}
for(int i=2; i<=n; i++){
    for(int j=1; j<=(n-i); j++){
        printf(" ");
    }
    for(int k=1; k<=(i*2)-1; k++){
        printf("*");
    }
    printf("\n");
}

return 0;
}

(23)
    1
   121
  12321
 1234321
123454321

123454321
 1234321
  12321
   121
    1

#include<stdio.h>
int main(){

int n;
printf("Enter a number:");
scanf("%d", &n);

for(int i=1; i<=n; i++){
    for(int j=1; j<=(n-i); j++){
        printf(" ");
    }
    for(int k=1; k<=i; k++){
        printf("%d", k);
    }
    for(int l=i-1; l>=1; l--){
        printf("%d", l);
    }
    printf("\n");
}

for(int i=1;i<=n;i++){
    for(int j=1; j<=(i-1); j++){
        printf(" ");
    }
    for(int k=1;k<=(n-(i-1)); k++){
        printf("%d", k);
    }
    for(int l=(n-i); l>=1;l--){
        printf("%d",l);
    }
    printf("\n");
}

return 0;
}
